# استخدم صورة PHP الرسمية مع سيرفر داخلي
FROM php:8.2-cli

# نسخ ملفات المشروع إلى المجلد /app داخل الحاوية
WORKDIR /app
COPY . .

# افتح المنفذ الذي سيستخدمه Render (عادة 10000 أو يحدد بمتغير $PORT)
EXPOSE 8080

# الأمر لتشغيل سيرفر PHP الداخلي
CMD ["php", "-S", "0.0.0.0:8080"]